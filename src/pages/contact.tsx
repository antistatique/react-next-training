import React from 'react';
import tw, { css } from 'twin.macro';

const Contact = (): JSX.Element => (
  <div>
    <h1 tw="text-blue-900 text-8xl">Contact page</h1>
  </div>
);

export default Contact;
